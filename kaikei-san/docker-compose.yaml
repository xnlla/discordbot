version: '3.9'
services:
  kaikeisan:
    restart: always
    build: .
    environment:
      TOKEN: ${TOKEN}
      GUILD_ID: ${GUILD_ID}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    volumes:
      - kaikeisan-data:/workspace/data

volumes:
  kaikeisan-data:
